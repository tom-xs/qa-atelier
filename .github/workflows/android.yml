name: Android UIAutomator

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  android-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Check for Android tests
        id: check
        run: |
          COUNT=$(find android/app/src/androidTest -name "*.kt" | wc -l)
          echo "count=$COUNT" >> $GITHUB_OUTPUT
          echo "Found $COUNT test files"

      - name: No tests found
        if: steps.check.outputs.count == 0
        run: echo "No Android tests yet, skipping."

      - uses: actions/setup-java@v4
        if: steps.check.outputs.count > 0
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Enable KVM
        if: steps.check.outputs.count > 0
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules && sudo udevadm trigger

      - name: Cache Gradle
        if: steps.check.outputs.count > 0
        uses: actions/cache@v4
        with:
          path: ~/.gradle/caches
          key: gradle-${{ hashFiles('android/**/*.gradle*') }}

      - name: Run Android tests
        if: steps.check.outputs.count > 0
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 33
          target: google_apis
          arch: x86_64
          script: cd android && ./gradlew connectedAndroidTest
